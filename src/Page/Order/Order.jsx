import Empty from "../../Components/Empty"

function Order(selectedItems) {
    console.log(selectedItems)
    return (
        <div>
            <Empty name={"order"}></Empty>
        </div>
    )
}

export default Order